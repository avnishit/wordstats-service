import wordStore from '../infra/wordStore';
export class queueTask {
    public static async execute(
        input: string,
        type: string
    ): Promise<any> {
            return null;
    }
}